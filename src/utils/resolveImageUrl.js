// Toutes les images viennent des données factices (imports Vite déjà résolus
// en URL absolue) — pas de concatenation avec une base d'API à faire.
export const resolveImageUrl = (image) => image || null;
